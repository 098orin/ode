[package]
name = "ode_app"
version.workspace = true
edition.workspace = true

[dependencies]
ode_backend = { path = "../ode_backend/" }
ode_core = { path = "../ode_core/" }
ode_smithay = { path = "../ode_smithay/" }
